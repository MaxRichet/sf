Commands for run wp :

```bash
    docker-compose up
    npm install -g @wordpress/env
    wp-env start 
```

For admin connexion :

    - add /wp-login in URI
    - user : admin
    - password : password